<?xml version="1.0" encoding="UTF-8" standalone="yes"?>
<features xmlns="http://karaf.apache.org/xmlns/features/v1.0.0"
  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://karaf.apache.org/xmlns/features/v1.0.0 http://karaf.apache.org/xmlns/features/v1.0.0"
  name="rmap-loader">
  <repository>mvn:org.apache.camel.karaf/apache-camel/${camel.version}/xml/features
  </repository>
  <repository>mvn:io.hawt/hawtio-karaf/${hawtio.version}/xml/features
  </repository>
  <repository>
    mvn:org.apache.activemq/activemq-karaf/${activemq.version}/xml/features
  </repository>
  <feature name="rmap-loader" description="rmap-loader" version="0.0.1.SNAPSHOT">
    <details>rmap-loader</details>

    <feature>scr</feature>
    <feature>webconsole</feature>
    
    <feature>activemq-broker</feature>

    <!-- Hawtio for monitoring -->
    <!-- 
    <feature version="${hawtio.version}">hawtio</feature>
    -->

    <!-- RMap individual features -->
    <feature version="${project.version}">rmap-loader-framework</feature>
    <feature version="${project.version}">rmap-loader-transform</feature>
    <feature version="${project.version}">rmap-loader-deposit</feature>
    <feature version="${project.version}">rmap-loader-extractor</feature>

  </feature>
</features>
